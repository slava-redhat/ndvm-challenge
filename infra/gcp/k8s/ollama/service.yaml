apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: ollama
  ports:
    - name: ollama
      port: 11434
      targetPort: ollama
