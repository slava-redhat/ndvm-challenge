.env
# NEVER commit credentials — Vertex ADC lives in the repo dir but stays untracked.
*application_default_credentials.json
*.json.cred
__pycache__/
*.pyc
data/pdfs/*.pdf
.venv/
.claude/

# docs excluded from this repo (design narrative kept locally)
CONTEXT.md
DESIGN.md
docs/
